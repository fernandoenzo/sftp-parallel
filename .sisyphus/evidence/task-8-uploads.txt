Task 8: Extract run_uploads() and collect_results() Functions

=== QA: Function count ===
$ grep -c 'run_uploads()\|collect_results()' /tmp/sftp-parallel-upload-v7.sh
2

=== QA: Syntax check ===
$ bash -n /tmp/sftp-parallel-upload-v7.sh
(no output = clean)

=== QA: Function signatures and call sites ===
Line 168: run_uploads() - takes buckets nameref, host, work_dir
Line 192: collect_results() - takes pids nameref, work_dir; echoes FAILED count
Line 220: run_uploads BUCKETS "$HOST" "$WORK_DIR"
Line 222: FAILED=$(collect_results PIDS "$WORK_DIR")

=== Key design decisions ===
- run_uploads() uses local -n nameref for buckets array
- PIDS is set globally inside run_uploads() (required by cleanup trap)
- collect_results() uses local -n nameref for pids array
- collect_results() echos FAILED count (captured via $())
- REMOTE_DIR and LOCAL_DIR used as globals inside run_uploads (same as original)
- CONNECT_TIMEOUT used as global readonly (same as original)

=== Logic preserved ===
- Same iteration over buckets, same batch creation, same sftp invocation, same PID tracking
- Same wait loop, same exit code capture, same error logging, same FAILED counting
