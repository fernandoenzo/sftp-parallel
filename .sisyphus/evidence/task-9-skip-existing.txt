Task 9: --skip-existing with size comparison - Evidence
=====================================================

## QA Scenarios

### parse_ls_output
$ python3 -c "from sftp_parallel.uploader import parse_ls_output; print(parse_ls_output('-rw-r--r-- 1 user group 1234 Jan  1 12:00 file.txt\n'))"
{'file.txt': 1234}

### filter_existing_files
$ python3 -c "from sftp_parallel.uploader import filter_existing_files; ..."
Need upload: ['b.txt']
(a.txt with matching size 100 was skipped, b.txt with different size was included)

### Unit Tests (69/69 pass)
tests/test_uploader.py::TestParseLsOutput - 7 tests PASS
tests/test_uploader.py::TestGetRemoteFileSizes - 3 tests PASS
tests/test_uploader.py::TestFilterExistingFiles - 5 tests PASS
tests/test_cli.py::TestSkipExisting - 4 tests PASS

## Changes Made

### src/sftp_parallel/uploader.py
- Added `parse_ls_output(ls_output: str) -> dict[str, int]`: Parses SFTP ls -l output
- Added `get_remote_file_sizes(host, remote_dir, timeout) -> dict[str, int]`: Gets remote file sizes via SFTP
- Added `filter_existing_files(local_dir, local_files, remote_sizes) -> list[str]`: Filters files needing upload
- Added `os` and `re` imports

### src/sftp_parallel/cli.py
- Updated imports to include `filter_existing_files` and `get_remote_file_sizes` from uploader
- Modified `_handle_upload()` to:
  - When `args.skip_existing` is True, call `get_remote_file_sizes()` 
  - Filter files using `filter_existing_files()`
  - Print "Skipping N existing files" message
  - Exit early with "All files already exist on remote" if all skipped

### tests/test_uploader.py
- Added TestParseLsOutput: 7 tests (single, multiple, empty, whitespace, spaces, non-matching, zero-size)
- Added TestGetRemoteFileSizes: 3 tests (parsed sizes, custom timeout, empty on failure)
- Added TestFilterExistingFiles: 5 tests (skip matching, include when missing, include when different, mixed, OSError skip)

### tests/test_cli.py
- Added TestSkipExisting: 4 tests (filters files, skip message, all exist on remote, not called without flag)